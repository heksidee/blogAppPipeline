{
  "name": "teht-11.20-blogapp-pipeline",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently --kill-others \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "lint:all": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test:all": "npm run test --prefix backend && npm run test --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
